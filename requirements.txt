# ── Core API ─────────────────────────────────────────────────────────────────
fastapi
uvicorn[standard]
pydantic
pydantic-settings
python-dotenv
python-multipart

# ── LLM / Agent (LangChain 1.x + LangGraph) ─────────────────────────────────
langchain
langchain-core
langchain-openai
langchain-community
langchain-text-splitters
langgraph
openai
tenacity

# ── Vector store ─────────────────────────────────────────────────────────────
faiss-cpu
numpy

# ── Document parsing / ingestion ────────────────────────────────────────────
pypdf
python-docx
openpyxl
pandas

# ── EDA / Regression / Anomaly detection ────────────────────────────────────
scipy
scikit-learn

# ── UI ───────────────────────────────────────────────────────────────────────
gradio
httpx
plotly

# ── Data fetching ────────────────────────────────────────────────────────────
yfinance

# ── Logging ──────────────────────────────────────────────────────────────────
loguru

# ── Evaluation (RAGAS) ───────────────────────────────────────────────────────
# Tested with: langchain 1.3.8, langchain-core 1.4.6, langchain-openai 1.3.0,
# langchain-community (latest, 1.x-compatible), ragas 0.4.3.
# NOTE: this version of ragas (vibrantlabsai fork) still imports
# langchain_community.chat_models.vertexai, which no longer ships in current
# langchain-community. See README "Troubleshooting" for the required stub
# module workaround. ragas >=0.2 also uses a different evaluate() signature
# (EvaluationDataset + explicit llm=/embeddings= wrappers) — already handled
# in evaluation/evaluator.py.
ragas
datasets

# ── Testing ──────────────────────────────────────────────────────────────────
pytest